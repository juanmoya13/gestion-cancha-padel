import { formatCurrency } from "@/lib/utils";
import type { getOperationHistory } from "../queries";

type History = Awaited<ReturnType<typeof getOperationHistory>>;
export function HistoryView({ history }: { history: History }) {
  return <div className="space-y-8"><section><h2 className="mb-3 text-lg font-semibold text-slate-900">Ventas</h2><div className="overflow-x-auto rounded-xl border border-slate-200"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-b bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3">Fecha</th><th className="px-4 py-3">Detalle</th><th className="px-4 py-3">Total</th><th className="px-4 py-3">Estado</th></tr></thead><tbody className="divide-y divide-slate-100">{history.sales.map((sale) => <tr key={sale.id} className={sale.status === "CANCELADO" ? "bg-slate-50 text-slate-400 line-through" : ""}><td className="px-4 py-3">{new Date(sale.date).toLocaleString("es-AR")}</td><td className="px-4 py-3">{sale.items.map((item) => `${item.product?.name ?? "Producto"} x${item.quantity}`).join(", ")}</td><td className="px-4 py-3 font-mono tabular-nums">{formatCurrency(sale.total_amount)}</td><td className="px-4 py-3">{sale.status}</td></tr>)}</tbody></table></div></section><section className="grid gap-6 lg:grid-cols-3"><div className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="font-semibold">Compras</h2><div className="mt-3 space-y-2 text-sm">{history.purchases.map((purchase) => <p key={purchase.id} className="flex justify-between"><span>{new Date(purchase.date).toLocaleDateString("es-AR")}</span><strong>{formatCurrency(purchase.total_amount)}</strong></p>)}</div></div><div className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="font-semibold">Movimientos de stock</h2><div className="mt-3 space-y-2 text-sm">{history.stockMovements.slice(0, 10).map((movement) => <p key={movement.id} className="flex justify-between"><span>{movement.movement_type}</span><strong>{movement.quantity_change}</strong></p>)}</div></div><div className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="font-semibold">Cuenta corriente</h2><div className="mt-3 space-y-2 text-sm">{history.accountMovements.slice(0, 10).map((movement) => <p key={movement.id} className="flex justify-between"><span>{movement.movement_type}</span><strong>{formatCurrency(movement.amount_change)}</strong></p>)}</div></div></section></div>;
}
