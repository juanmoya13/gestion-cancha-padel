import { ModulePlaceholder } from "@/app/(dashboard)/_components/module-placeholder";

export default function AccountPage() {
  return <ModulePlaceholder title="Cuenta corriente" description="Aquí podrás registrar cobros, ajustes y consultar el historial de cada cuenta." />;
}